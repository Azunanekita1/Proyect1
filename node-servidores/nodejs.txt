
verificar instalacion
node -v
npm -v

SERVIDOR BASICO 
API http responde solicitudes de clientes (navegador,aplicaciones) con recursos html,json etc.
node incluye el modulo http para crear servidores
LIBRERIA EXPRESS
importar http 
funcion de flecha recibe contenido lo pone a correr.

//ctrl + c para pararlo en la consola.
se debe ejecutar en la consola

Servidor con contenido estatico: html,css o imagenes

modulo fs para leer archivos
el modulo fs permite leer y envair al clientes

LINEA DE COMANDOS:
ls o dir = lista los archivos y carpetas del directorio actual
mkdir =crea una nueva carpeta
ls - ls = que permisos tiene (linux)
type nul > archivo crea un nuevo archivo.
touch =crear archivo 
rm o del index.js =elimina un archivo

NPM = 
npm install nombre-paquete
npm install -g nombre-paquete //global 

npm init 
npm install chalk
npm install chalk@4

nodemon para reiniciar automaticamente el servidor al guardar.

npm install -g nodemon
usar nodemon para ejecutar src/index.js

nodemon src/index.js

npm install figlet = para crear texto ASCII 

